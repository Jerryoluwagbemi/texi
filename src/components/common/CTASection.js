import React from "react";

const CTASection = () => (
  <section style={{ background: "#f7f7f7", color: "#111", padding: "3rem 0", textAlign: "center" }}>
    <h2 style={{ fontFamily: "'Jost', Arial, sans-serif", fontWeight: 700, fontSize: 28, marginBottom: 18, color: "#111" }}>
      Ready to book your next home service?
    </h2>
    <button style={{ background: "#000", color: "#fff", fontFamily: "'Jost', Arial, sans-serif", fontWeight: 700, fontSize: 18, border: 0, borderRadius: 6, padding: "12px 32px", cursor: "pointer" }}>
      Join Texi
    </button>
  </section>
);

export default CTASection;
